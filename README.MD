# ConnectFour with MVVM

This is an implementation of a connect 4 style game that uses
AndroidX/Jetpack ViewModel to implement a MVVM pattern and
uses ViewModel to save state across orientation changes.

